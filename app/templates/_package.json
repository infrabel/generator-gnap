{
    "name": "<%= _.slugify(_.humanize(appName)) %>",
    "description": "<%= appTitle %>",
    "version": "0.0.0",
    "private": true,
    "dependencies": {
        "<%= themeName %>": "*"
    }
}
